# macOS system files
.DS_Store
**/.DS_Store

docs/prompts/legacy-prompts/

# Chat History - Local conversation logs and job history summaries
chat-history/
job-summaries/

# Job History Summaries - Local job history summaries
job-history/

# Keep the .gitkeep file to maintain the directory structure
!chat-history/.gitkeep
!job-history/.gitkeep

# JD Cache (Job Fit Analyzer) - Cache parsed JDs locally, don't commit
jd_parsed/*.json
docs/plans/archive/
jd_parsed/*_parsed.json
jd_parsed/*_v*_parsed.json

# Keep the README
!jd_parsed/README.md

# Node.js dependencies
node_modules/
/.pnp
.pnp.js

# Build output
/dist
/build
.vite

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
.idea
.vscode
*.swp
*.swo
*~

# OS files
Thumbs.db
job-summary-update-v8.0.md

# Ignore the n8n-builder (separate git project)
# This is a sibling directory that has its own git repo
../n8n-builder/

# Local AI context files (Codex integration)
.codex-ai-context/

# Local research and archive (working files, historical reference)
.local-research/
.local-archive/
