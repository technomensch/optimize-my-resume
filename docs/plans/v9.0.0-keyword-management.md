# v9.0.0 - Keyword Management & Validation

**Branch:** `v9.0.0-keyword-management`
**Target Release:** v9.0.0
**Type:** Feature
**Priority:** High
**Status:** Complete

---

## Issues Included

- **Issue #67** - Interactive Keyword Management UI
- **Issue #69** - Custom Keyword Validation & Evidence Check (depends on #67)

---

## Overview

Implement interactive keyword management UI allowing users to toggle keywords between USE/IGNORE lists, and add custom keywords with evidence validation guardrails.

---

## Implementation Sequence

**CRITICAL:** These must be done sequentially due to dependency.

### 1. Issue #67 - Interactive Keyword Management UI
**First** - No dependencies

**Changes:**
- Add state: `keywordsToUse`, `keywordsToIgnore`
- Create two-column UI with click-to-toggle functionality
- Add custom keyword input field
- Wire up to existing `generateCustomizedContent()` function

**Files Modified:**
- `claude-artifacts/Should-I-Apply-webgui.jsx`

**Solution Approach:** `docs/issues/issue-67/solution-approach.md`
**Test Cases:** `docs/issues/issue-67/test-cases.md`

---

### 2. Issue #69 - Custom Keyword Validation & Evidence Check
**Second** - Depends on #67 being complete

**Changes:**
- Add evidence validation logic for custom keywords
- Yellow warning indicators for unevidenced keywords
- Confirmation modal before generation
- "Light integration" for confirmed-but-unevidenced keywords
- New Guardrail #32 definition

**Files Modified:**
- `claude-artifacts/Should-I-Apply-webgui.jsx`
- `optimization-tools/narrative-generator/ng_summary-generation.md` (Shadow Sync)
- `optimization-tools/resume-analyzer/ra_quality-gates-guardrails.md` (Guardrail #32)
- `PROJECT-INSTRUCTIONS.md` (Gold Master sync)
- `Project-GUI-Instructions.md` (Entrypoint sync)

**Solution Approach:** `docs/issues/issue-69/solution-approach.md`
**Test Cases:** `docs/issues/issue-69/test-cases.md`

---

## Shadow Sync Protocol

For Issue #69, follow Shadow Sync Protocol:

1. **MODULE:** `ng_summary-generation.md` - Add user keyword preference logic
2. **GOLD MASTER:** `PROJECT-INSTRUCTIONS.md` - Sync changes
3. **ENTRYPOINT:** `Project-GUI-Instructions.md` - Verify consistency

Add this to `ng_summary-generation.md`:
```xml
<user_keyword_preferences>
  IF the user provides a list of specific keywords to USE or IGNORE:
  1. **Strictly Enforce:** Do not use any keyword from the "IGNORE" list.
  2. **Prioritize:** Ensure valid keywords from the "USE" list are integrated (if evidence exists).
  3. **Custom Keywords:** If user adds a keyword not in the JD, treat it as a high-priority "USE" keyword (subject to evidence validation).
</user_keyword_preferences>
```

---

## Branch Creation

```bash
git checkout main
git pull origin main
git checkout -b v9.0.0-keyword-management
```

---

## Verification Steps

1. Test keyword toggle (USE ↔ IGNORE)
2. Add custom keyword and verify validation logic
3. Attempt to generate with unevidenced keyword - confirm warning modal appears
4. Confirm unevidenced keywords get "light integration" treatment
5. Verify Shadow Sync: Check all three files for consistency
6. Run test cases from both issue folders

---

## Success Criteria

- [ ] Issue #67 test cases pass
- [ ] Issue #69 test cases pass
- [ ] Shadow Sync complete (3 files consistent)
- [ ] Guardrail #32 documented
- [ ] No regression in existing features
- [ ] Ready to merge to main and tag as v9.0.0
